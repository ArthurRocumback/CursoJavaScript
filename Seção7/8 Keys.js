let key = {
    /* Chave da propriedade */ 'chave 10' /* Valor da propriedade */: 10,
    'chave 20' : 20,
    'chave 30' : 30,
}
console.log(key);
// Retorna qual a chave da propriedade
console.log(Object.keys(key));