let numeros = [10, 20, 30, 40, 50];

for (let i = 0; i < numeros.length; i++) {
    // ${i}: ${numeros[i]} SUBSTITUI A CONCATENAÇÃO de "Item" + i + ": " + numeros[i]
    console.log(`Item ${i}: ${numeros[i]}`);
}
